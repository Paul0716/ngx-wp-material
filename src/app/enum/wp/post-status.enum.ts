/**
 * wordpress post status
 *
 * @export
 * @enum {number}
 */
export enum PostStatus {
  publish = 'publish',
  future  = 'future',
  draft   = 'draft',
  pending = 'pending',
  private = 'private',
}
